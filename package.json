{
  "name": "qanim",
  "version": "1.0.0",
  "description": "Qanim Agent",
  "main": "./dist/index.js",
  "scripts": {
    "start": "npm run build && http-server",
    "build": "tsc && browserify ./dist/index.js --s QanimLibrary -o ./dist/bundle.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "uuid": "^7.0.2"
  },
  "devDependencies": {
    "@types/chrome": "0.0.101",
    "@types/uuid": "^7.0.2"
  }
}
